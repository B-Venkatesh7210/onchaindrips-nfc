# Sui wallet: Bech32 (Phantom/Sui Wallet, e.g. suiprivkey1...) or 64-char hex (with or without 0x)
PRIVATE_KEY=suiprivkey1... or 0x...

# RPC (testnet default)
RPC_URL=https://fullnode.testnet.sui.io

# After running publish.ts, set these from the script output
PACKAGE_ID=
ADMIN_CAP_OBJECT_ID=

# For createDrop.ts (no Walrus for drop)
DROP_NAME=My Drop
COMPANY_NAME=Bybit
EVENT_NAME=Token2049 Dubai 2026
DROP_TOTAL_SUPPLY=100

# For mintShirts.ts (after create-drop). drop has all drop details; pass image + metadata blob ids (hex, no 0x)
DROP_OBJECT_ID=
MINT_COUNT=100
WALRUS_BLOB_ID_IMAGE=
WALRUS_BLOB_ID_METADATA=

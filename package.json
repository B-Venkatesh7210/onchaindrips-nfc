{
  "name": "onchaindrips-nfc",
  "private": true,
  "scripts": {
    "build": "pnpm -r run build",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build:contracts": "cd contracts/onchaindrips && sui move build"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=20"
  }
}

import { Raleway } from "next/font/google";

// Global font instances to reuse across the app

export const ralewayBlackItalic = Raleway({
  subsets: ["latin"],
  weight: "900",
  style: "italic",
});


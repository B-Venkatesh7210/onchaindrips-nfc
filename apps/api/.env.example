# Sui RPC (testnet default)
RPC_URL=https://fullnode.testnet.sui.io

# Package ID (from publish script)
PACKAGE_ID=

# Sponsor key: Bech32 (suiprivkey1...) or 64-char hex. Used only on backend for gas.
SPONSOR_PRIVATE_KEY=

# Walrus (optional): publisher for uploads, aggregator for reads. Defaults: testnet public services.
# WALRUS_PUBLISHER_URL=https://publisher.walrus-testnet.walrus.space
# WALRUS_AGGREGATOR_URL=https://aggregator.walrus-testnet.walrus.space

SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Admin: create drop / mint shirts (optional). Admin routes require X-Admin-Address header matching ADMIN_ADDRESS.
ADMIN_CAP_OBJECT_ID=
ADMIN_ADDRESS=

# Short NFC claim tokens (≤14 chars) are stored in Supabase table claim_url_tokens. Run migration 003_claim_url_tokens.sql.

# Bidding uses Yellow Network sandbox (Sepolia + ytest.usd). Settlement is done client-side via Yellow channels.
+